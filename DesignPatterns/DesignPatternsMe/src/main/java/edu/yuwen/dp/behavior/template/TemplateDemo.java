package edu.yuwen.dp.behavior.template;

/**
 * 2.模板模式（行为型）
 * 
 * 在模板模式经典的实现中，模板方法定义为 final，可以避免被子类重写。
 * 需要子类重写的方法定义为 abstract，可以强迫子类去实现。
 * 不过，在实际项目开发中，模板模式的实现比较灵活，以上两点都不是必须的。
 * 模板模式有两大作用：复用和扩展。
 *
 * 没有Demo，可以参考InputStream的read方法,对应的模板方法是read(byte b[], int off, int len)
 */
public class TemplateDemo {

}
